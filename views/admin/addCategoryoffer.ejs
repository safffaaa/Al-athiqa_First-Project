 <%- include('./head') %>

    <div class="row my-5 d-flex justify-content-center">
        <div class="col-6 p-3 bg-white shadow-sm">
            <h1 class="fw-bold text-center text-secondary">ADD OFFERS</h1>
            <hr>
            <% if (messages.success) { %>
              <div class="alert alert-success">
                  <%= messages.success %>
              </div>
          <% }else if (messages.error){ %>
            <div class="alert alert-danger">
              <%= messages.error %>
          </div>
          <%}%>
    
            <div class="card p-2">
                <form method="post" action="/addOffer">                     
                    <div class="form-group">
                        <label>Category</label>
                        <select name="name" class="form-control" required>
                            <option value="" selected disabled>Select a Category</option>
                            <% selectCategory.forEach((x) => { %>
                                <option value="<%=x.name %>"><%=x.name %> </option>                          
                            <% }) %> 
                        </select>
                    </div>                  
                                       
                    <div class="form-group">
                      <label>Discount Percentage</label>
                      <input type="number" class="form-control" name="discount" required>
                    </div>
                    <div class="form-group">
                      <label>Start From</label>
                      <input type="datetime-local" class="form-control" id="valid_from" name="valid_from" required>
                    </div>
                    <div class="form-group">
                      <label>Ends On</label>
                      <input type="datetime-local" class="form-control" id="valid_to" name="valid_to" required >
                      <p id="dateValidationMessage" class="text-danger"></p>
                    </div>
                    <div class="form-group text-end p-2">
                      <input type="submit" class="btn btn-outline-dark" value="Add Offer" onclick="return validateDates()">
                    </div>
                </form>
            </div>
        </div>
    </div>

  


